<%= form_for(@incoming_call) do |f| %>
  <% if @incoming_call.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@incoming_call.errors.count, "error") %> prohibited this incoming_call from being saved:</h2>

      <ul>
      <% @incoming_call.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
